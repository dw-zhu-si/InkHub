# 墨枢 InkHub 隐私说明

生效日期：2026-09-07  
适用版本：墨枢 InkHub 0.3.1  
提供者：野路子工作室  
联系邮箱：`n_sir@sina.com`

## 1. 适用范围

本说明适用于墨枢桌面应用本身。通过墨枢连接的模型供应商、ModelHub、GitHub、Apple 和用户主动打开的其他网站，分别适用其自身的隐私政策与账户条款。

## 2. 本地优先

墨枢不要求注册墨枢账号。小说、工作区、全文索引、阅读进度、创作配置、Agent / Skill 配置、对话历史、模型用量记录和生成的封面/插图默认保存在用户设备或用户选择的目录中。

添加现有小说时，墨枢会读取用户选择目录中的 Markdown 或 TXT 等受支持文件，并在应用数据目录中建立本机索引。墨枢不会仅因添加目录就自动把小说上传到野路子工作室运营的服务器。

AI 修复会先生成审阅稿。只有用户继续确认写回时，应用才修改对应来源文件；写回前会在本机应用数据目录中保存备份。

## 3. 模型服务与内容传输

只有在用户配置模型并主动执行 AI 操作时，墨枢才向所选模型端点发起请求。请求可能包含：

- 用户输入的提示词、聊天消息和对话上下文；
- 用户主动附加的文本、图片或编辑器正文引用；
- 为质检、修复或续写而选择的小说章节、摘要、结构化设定和问题信息；
- 为封面生成提供的书名、作者名、视觉方向和风格提示；
- 为插图生成提供的场景提示；
- 为执行创作方法而附带的 Agent、Skill 和工具描述。

如果选择云端厂家，数据直接发送到用户配置的厂家 API 地址。墨枢不控制供应商的日志、保留、训练、跨境传输或账户关联方式；用户应在发送未公开作品前阅读并接受对应供应商政策。

如果选择 ModelHub，墨枢只允许连接本机 `127.0.0.1:11435` 或等价 `localhost`。ModelHub 后续是否把请求转发到云端以及如何处理数据，取决于用户在 ModelHub 中的配置。

如果选择 Ollama、LM Studio 或其他本机回环服务，请求保留在本机网络边界内；该服务自身的插件或配置仍可能产生网络请求，需要用户自行核对。

拉取模型列表或执行连接测试也会联系所选模型服务，并可能发送 API Key。连接测试可能产生供应商费用。

## 4. 凭据与配置

模型 API Key 和 ModelHub 项目网关令牌保存在本机，并使用 Electron `safeStorage` 调用系统安全存储能力加密。系统安全存储不可用时，应用不会把明文密钥降级写入普通配置文件。

模型名称、厂家、模型 ID、API 地址、启停状态和用量摘要属于非秘密配置，保存在本机应用数据目录。用户不应把密钥写进 API URL、小说正文、提示词、截图或公开反馈。

## 5. 默认关闭的通道

0.3.1 的发布代码默认关闭 DeepWrite 公共模型目录、软件 token 用量上报、技能市场、云备份和应用公告通道。独立自动更新源也未配置，因此点击检查更新不会连接墨枢更新服务器。

本版本未集成开发者运营的广告、跨应用跟踪、第三方分析或崩溃上报服务。操作系统、App Store、GitHub 和模型供应商可能按其自身政策处理下载、网络和账户信息。

## 6. 文件、系统权限与安全边界

应用通过用户主动选择目录或文件获得作品与工作区访问。Mac App Store 构建会把用户选择目录的 security-scoped bookmark 和对应绝对路径保存在本机应用数据目录，用于下次启动继续访问；文件权限设为仅当前用户可读写。应用主窗口启用上下文隔离、禁用 Node.js 集成并启用 Renderer 沙盒；运行时默认拒绝网页权限请求。应用没有为摄像头、麦克风、定位、通讯录或蓝牙声明业务用途。

用户可以把文本复制到系统剪贴板，也可以从剪贴板或文件选择器附加内容。剪贴板与文件内容只有在用户执行对应操作时才会被读取或写入。

## 7. 数据选择与删除

用户可以在模型配置中删除不再使用的模型配置和加密密钥。当前代码会在本机保存对话历史，但没有足够证据证明界面已经提供覆盖所有历史的完整删除入口；需要彻底删除时，应按下述方式清理应用数据。用户选择的小说源文件、导出文件、封面和插图位于用户目录中，应由用户自行备份和删除。

卸载应用可能保留应用数据目录，以避免误删作品索引、配置和恢复备份。需要彻底清理时，应先备份仍需保留的作品和修复备份，再删除操作系统为墨枢分配的应用数据目录。具体路径随平台和安装方式变化；不确定时请通过支持邮箱索取当前版本指引。

删除本机数据不会自动删除已发送到模型供应商的数据。供应商侧的查询、导出或删除请求应按该供应商的账户与隐私流程办理。

## 8. 支持沟通

用户通过邮件或 GitHub 主动联系时，邮件地址、GitHub 账号、消息内容和附件由对应平台处理，并可被野路子工作室用于答复、排查问题和改进产品。请不要发送密钥、小说全文或不必要的个人信息。支持数据的具体保留期限尚未形成独立公开制度；相关内容同时受邮件或 GitHub 平台政策约束。

## 9. 儿童

墨枢不是面向儿童设计的在线社交服务，也不主动要求儿童提供个人信息。创作内容和模型输出可能取决于用户提示词与供应商能力，监护人应根据使用环境进行监督。

## 10. 变更

如果未来启用账号、云同步、遥测、在线市场、开发者更新服务或新的第三方 SDK，本说明与 App Store 隐私回答必须在功能发布前同步更新。重要变化将通过公开发布说明或隐私页面告知。

## 11. 联系

隐私问题请联系：`n_sir@sina.com`。
